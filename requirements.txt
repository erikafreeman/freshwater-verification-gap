# Minimum versions needed by reproduce_figures.ipynb.
# We pin lower bounds rather than exact versions so Binder and Colab can
# resolve binary wheels on their own platforms. The notebook uses only
# standard pandas/matplotlib/numpy idioms that have been stable for years.
pandas>=2.0,<4
matplotlib>=3.7,<4
numpy>=1.24,<3
requests>=2.28
